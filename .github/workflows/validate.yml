name: Validate

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  prompt-consistency:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Verify agent interface prompt consistency
        run: ruby ./scripts/check-agent-interface-consistency.rb
